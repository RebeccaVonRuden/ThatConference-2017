**Local and global variables

*Global Variables
num = 42  -- All numbers are doubles.
s = 'A String'     -- Immutable strings (AKA trings that can not be changed!)
t = "Another String" -- Immutable strings (AKA Strings that can not be changed!)
t = nil  -- Undefines a variable (Will be garbage collected!)
b = true -- boolean 

*Local Variables
local num = 42  -- All numbers are doubles.
local s = 'A String'     -- Immutable strings (AKA trings that can not be changed!)
local t = "Another String" -- Immutable strings (AKA Strings that can not be changed!)
local t = nil  -- Undefines a variable (Will be garbage collected!)
local b = true -- boolean     

**If conditions
if num > 40 then
    print('over 40')
elseif num == 40 then
    print('Equals 40')
else
    print('Less than 40')
end

if s == 'A String" then
    print('s equals A string')
else if s ~= 'that confrence' then
    print('you are not at that confrence')
end

**Loops
while num < 50 do
    num = num + 1
    print(num)
end

for i = 1, 100 do
    karlSum = karlSum + i
end

**Functions
function fib(n)
    if n < 2 than return 1 end
    return fib(n-2) + fib(n-1)
end

**Array
a={} --new array
for i=1, 1000 do
    a[i] = 0 -- sets all values in  the array to 0
end

**Passing in Arguments
function main()

end

local args ={ ... }

main(arges[1])

**ComputerCraft and the Turtle API



** Comments 

-- Two dashes start a one-line comment.

--[[
     Adding two ['s and ]'s makes it a
     multi-line comment.
--]]
